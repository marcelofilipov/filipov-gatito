import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  fill: {
    flex: 1,
  }
});

export const colors = {
  roxo: '#a050be',
  laranja: '#fabe50',
  escuro: '#555555',
  claro: '#fff',
  cinza: '#c7c7c7',
};